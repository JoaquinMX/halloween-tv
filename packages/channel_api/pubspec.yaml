name: channel_api
description: "Plugin SPI for Halloween TV channels."
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.1
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  core_cache:
    path: ../core_cache
  core_playlist:
    path: ../core_playlist
  core_effects:
    path: ../core_effects
  core_foundation:
    path: ../core_foundation

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
