name: channel_retro_tv
description: "Retro horror channel placeholder."
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.1
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  channel_api:
    path: ../channel_api

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
